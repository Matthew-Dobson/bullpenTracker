import React from 'react'
import ReactDOM from 'react-dom/client'
import StrikeZone from './App.tsx'
import './index.css'   // keep this!

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <StrikeZone />
  </React.StrictMode>
)