# Bullpen Accuracy Tracker

**Goal:** Build a clean, professional full‑stack project to log bullpen pitch locations and generate per‑session stats and a PDF report.

## Project Slices
- Slice A — Git + folders (this commit):  
  - Create `/web` (React + TypeScript) and `/api` (FastAPI).  
  - Add `.gitignore`, `.gitattributes`, and this `README.md`.  
  - Commit.

- Slice B — Minimal scaffolds:  
  - Create React + TS app in `/web`.  
  - Create FastAPI app in `/api`.  
  - Document how to run each (README updates).  
  - Commit.

- Slice C — Strike‑zone UI MVP (web)
- Slice D — API endpoints MVP (api)
- Slice E — Session PDF export, stats & heatmap
- Slice F — Polish: linting, pre-commit hooks, CI, packaging

> Commit after each slice.
